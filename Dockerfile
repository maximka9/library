# Указываем базовый образ, в котором будет выполняться приложение
FROM openjdk:11-jre

# Создаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем JAR-файл приложения из папки target/classes в контейнер
COPY ./my-spring-app.jar /app.jar

# Запускаем приложение при старте контейнера
CMD ["java", "-jar", "my-spring-app.jar"]